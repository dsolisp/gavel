{
  // GitHub Copilot / VS Code customization discovery for gavel.
  // Copilot only scans .github/{skills,agents,prompts} by default. Gavel keeps
  // its canonical skills/, companion/skills/, and agents/ at the repo root, so
  // we point Copilot at those directly — no duplication, no extra sync surface.
  //
  // Safe as a committed workspace override: these chat.*Locations settings are
  // object-typed, so VS Code merges them key-by-key across scopes (built-in
  // defaults + user + workspace). This file only ADDS workspace-relative roots;
  // it never removes a user's global roots (e.g. ~/.copilot/skills,
  // ~/.claude/skills) or Copilot's built-in .github/* scan — a root is dropped
  // only if a key is explicitly set to false. Keys here are additive-only.
  "chat.agentSkillsLocations": {
    ".github/skills": true,
    ".claude/skills": true,
    "skills": true,
    "companion/skills": true
  },
  "chat.agentFilesLocations": {
    ".github/agents": true,
    ".claude/agents": true,
    "agents": true
  },
  "chat.promptFilesLocations": {
    ".github/prompts": true
  },
  "chat.instructionsFilesLocations": {
    ".github/instructions": true,
    ".github/copilot-instructions.md": true
  },
  // Monorepo case: if gavel is opened as a subfolder (e.g. inside a larger
  // workspace) rather than the repo root, let Copilot discover these from the
  // parent repository root as well.
  "chat.useCustomizationsInParentRepositories": true
}
