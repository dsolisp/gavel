// Utility script — not a test or locator file.
// gavel-ignore appears here as a marker in a helper, not an actual suppression.

function loadConfig() {
  return { suppressed: true };
}

module.exports = { loadConfig };
