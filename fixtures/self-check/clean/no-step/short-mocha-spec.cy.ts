// @ts-nocheck
// Clean: short mocha spec with it() calls — under 80 lines, no no-step violation.
describe('Example', () => {
  it('first scenario', () => {
    // test body
  });

  it('second scenario', () => {
    // test body
  });
});
