import { test } from '@playwright/test';

test('creates customer', async () => {
  const nationalId = 'synthetic-fixture-value';
  const accountNumber = 'synthetic-fixture-value';
  void nationalId;
  void accountNumber;
});
